import React, { useState } from 'react';
import { Phone, MapPin, Utensils, Star, Calendar, ChevronRight, CheckCircle, ArrowRight } from 'lucide-react';

const App = () => {
  // State untuk navigasi halaman
  const [activePage, setActivePage] = useState('home');

  // Data Paket Menu
  const aqiqahPackages = [
    {
      name: "Paket Hemat",
      price: "Rp 2.250.000",
      details: ["1 Ekor Kambing", "Minimal 400 Tusuk Sate", "1 Panci Gule", "Acar & Sambal"],
      recommended: false
    },
    {
      name: "Paket Sedang",
      price: "Rp 2.500.000",
      details: ["1 Ekor Kambing", "Minimal 500 Tusuk Sate", "1 Panci Gule", "Acar, Sambal & Bumbu"],
      recommended: true
    },
    {
      name: "Paket Besar",
      price: "Rp 3.000.000",
      details: ["1 Ekor Kambing", "Minimal 700 Tusuk Sate", "1 Panci Jumbo atau 1.5 Panci Gule", "Acar, Sambal & Bumbu"],
      recommended: false
    },
    {
      name: "Paket Jasa Masak",
      price: "Rp 850.000",
      details: [
        "Kambing Bawa Sendiri",
        "Jumlah Sate & Gule tergantung besar kambing",
        "Sudah termasuk Acar, Sambal & Bumbu",
        "Ada biaya tambahan masak jika > 700 tusuk & > 1 Panci Jumbo"
      ],
      recommended: false
    }
  ];

  const eventPackages = [
    {
      name: "Jasa Masak Kambing Guling",
      price: "Rp 1.200.000 / ekor",
      desc: "Kambing dari Anda kami bakar matang dari dapur, lalu dibawa utuh ke lokasi. Tim kami melayani pengirisan dan pemanasan di acara. Sudah termasuk kecap khusus, acar, dan sambal.",
      minOrder: "Kambing Bawa Sendiri"
    },
    {
      name: "Kambing Guling (Dari Kami)",
      price: "Rp 2.500.000 / ekor",
      desc: "Kambing pilihan dari kami yang dibakar matang dari dapur, lalu dibawa utuh ke lokasi. Tim kami melayani pengirisan dan pemanasan di acara. Sudah termasuk kecap khusus, acar, dan sambal.",
      minOrder: "Kambing Dari Kami"
    }
  ];

  // Menggabungkan semua nama paket untuk dropdown
  const allPackageNames = [
    ...aqiqahPackages.map(pkg => pkg.name),
    ...eventPackages.map(pkg => pkg.name),
    "Konsultasi / Paket Lainnya"
  ];

  // Komponen Navigasi
  const Navbar = () => (
    <nav className="bg-emerald-900 text-white sticky top-0 z-50 shadow-lg">
      <div className="max-w-6xl mx-auto px-4 py-3 md:py-4 flex justify-between items-center">
        <div 
          className="text-xl md:text-2xl font-serif font-bold tracking-wider cursor-pointer flex items-center gap-2"
          onClick={() => setActivePage('home')}
        >
          <Utensils className="text-amber-400 w-5 h-5 md:w-6 md:h-6" />
          CAKSABAR
        </div>
        
        {/* Desktop Nav */}
        <div className="hidden md:flex space-x-8 text-lg font-medium">
          <button 
            onClick={() => setActivePage('home')} 
            className={`hover:text-amber-400 transition ${activePage === 'home' ? 'text-amber-400 border-b-2 border-amber-400' : ''}`}
          >
            Beranda
          </button>
          <button 
            onClick={() => setActivePage('menu')} 
            className={`hover:text-amber-400 transition ${activePage === 'menu' ? 'text-amber-400 border-b-2 border-amber-400' : ''}`}
          >
            Paket & Menu
          </button>
          <button 
            onClick={() => setActivePage('contact')} 
            className={`hover:text-amber-400 transition ${activePage === 'contact' ? 'text-amber-400 border-b-2 border-amber-400' : ''}`}
          >
            Pemesanan
          </button>
        </div>

        {/* Mobile Header CTA */}
        <div className="md:hidden">
          <button onClick={() => setActivePage('contact')} className="bg-amber-500 text-emerald-900 px-3 py-1.5 rounded-lg text-xs font-bold shadow-md">
            Pesan Sekarang
          </button>
        </div>
      </div>

      {/* Mobile Bottom Nav */}
      <div className="md:hidden fixed bottom-0 left-0 right-0 bg-emerald-900 border-t border-emerald-800 flex justify-around py-3 text-[10px] uppercase tracking-widest z-[60]">
        <button onClick={() => setActivePage('home')} className={`flex flex-col items-center gap-1 ${activePage === 'home' ? 'text-amber-400 font-bold' : 'text-stone-300'}`}>
          <Star size={18} /> Beranda
        </button>
        <button onClick={() => setActivePage('menu')} className={`flex flex-col items-center gap-1 ${activePage === 'menu' ? 'text-amber-400 font-bold' : 'text-stone-300'}`}>
          <Utensils size={18} /> Menu
        </button>
        <button onClick={() => setActivePage('contact')} className={`flex flex-col items-center gap-1 ${activePage === 'contact' ? 'text-amber-400 font-bold' : 'text-stone-300'}`}>
          <Phone size={18} /> Kontak
        </button>
      </div>
    </nav>
  );

  // Halaman 1: Beranda
  const HomePage = () => (
    <div className="animate-fade-in pb-20 md:pb-0">
      <div className="relative bg-stone-100 py-12 md:py-20 lg:py-32">
        <div className="max-w-6xl mx-auto px-4 flex flex-col md:flex-row items-center gap-10 md:gap-12">
          {/* Text Section */}
          <div className="w-full md:w-1/2 space-y-5 md:space-y-6 text-center md:text-left order-2 md:order-1">
            <div className="flex justify-center md:justify-start">
              <span className="inline-flex items-center gap-2 bg-white border border-emerald-200 text-emerald-800 px-4 py-1.5 rounded-full font-bold text-[10px] md:text-xs tracking-widest shadow-sm uppercase">
                <CheckCircle size={14} className="text-emerald-500 fill-emerald-100" />
                <span>Halal & Higienis</span>
              </span>
            </div>
            <h1 className="text-3xl md:text-5xl lg:text-6xl font-serif font-bold text-stone-800 leading-tight">
              Sajian Sate Kambing <span className="text-emerald-700">Legendaris</span> Untuk Momen Sakral Anda.
            </h1>
            <p className="text-base md:text-lg lg:text-xl text-stone-600 leading-relaxed max-w-xl mx-auto md:mx-0">
              Spesialis Sate & Gule untuk Aqiqah, Pernikahan, dan Acara Besar. 
              Daging empuk, tanpa bau prengus, dengan resep warisan leluhur.
            </p>
            <div className="flex flex-col sm:flex-row gap-3 md:gap-4 pt-4">
              <button 
                onClick={() => setActivePage('menu')}
                className="bg-emerald-800 hover:bg-emerald-900 text-white px-6 md:px-8 py-3.5 md:py-4 rounded-xl text-base md:text-lg font-semibold shadow-lg transition flex items-center justify-center gap-2"
              >
                Lihat Paket Pemesanan <ArrowRight size={18} />
              </button>
              <button 
                onClick={() => setActivePage('contact')}
                className="bg-white border-2 border-emerald-800 text-emerald-800 hover:bg-emerald-50 px-6 md:px-8 py-3.5 md:py-4 rounded-xl text-base md:text-lg font-semibold transition"
              >
                Konsultasi Gratis
              </button>
            </div>
          </div>

          {/* Image Section */}
          <div className="w-full md:w-1/2 flex justify-center order-1 md:order-2">
             <div className="relative w-64 h-64 sm:w-80 sm:h-80 md:w-96 md:h-96 bg-amber-100 rounded-full p-4 sm:p-6 md:p-8 shadow-2xl flex items-center justify-center">
                <div className="w-full h-full bg-emerald-900 rounded-full flex items-center justify-center overflow-hidden border-4 border-white">
                   <img 
                      src="logoku" 
                      alt="Logo CAKSABAR" 
                      className="w-full h-full object-cover"
                      onError={(e) => {
                        e.target.onerror = null; 
                        e.target.src="https://via.placeholder.com/600x600/064e3b/fbbf24?text=Logo+CAKSABAR";
                      }}
                   />
                </div>
                {/* Decorative Element */}
                <div className="absolute -bottom-2 -right-2 bg-amber-400 text-emerald-900 px-4 py-2 rounded-full font-bold text-xs md:text-sm shadow-lg border-2 border-white">
                   Kualitas No.1
                </div>
             </div>
          </div>
        </div>
      </div>

      {/* Keunggulan Section */}
      <div className="bg-white py-16 md:py-24">
        <div className="max-w-6xl mx-auto px-4">
          <div className="text-center mb-12 md:mb-16">
            <h2 className="text-2xl md:text-4xl font-serif font-bold text-stone-800">Mengapa Memilih CAKSABAR?</h2>
            <div className="w-20 h-1 bg-amber-400 mx-auto mt-4 rounded-full"></div>
          </div>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-8">
            {[
              { icon: CheckCircle, title: "Sesuai Syariat", desc: "Proses penyembelihan dan pengolahan dijamin Halal dan sesuai tuntunan syariat Islam." },
              { icon: Star, title: "Kualitas Premium", desc: "Menggunakan kambing muda pilihan. Daging empuk, bumbu meresap, dan tidak bau prengus." },
              { icon: Calendar, title: "Tepat Waktu", desc: "Jaminan pengiriman tepat waktu untuk acara penting Anda. Makanan tiba dalam kondisi hangat." }
            ].map((item, i) => (
              <div key={i} className="p-8 bg-stone-50 rounded-2xl border border-stone-100 text-center hover:shadow-xl transition-all duration-300">
                <div className="w-16 h-16 bg-emerald-100 rounded-full flex items-center justify-center mx-auto mb-6 transform rotate-3">
                  <item.icon className="text-emerald-700" size={32} />
                </div>
                <h3 className="text-xl font-bold text-stone-800 mb-3">{item.title}</h3>
                <p className="text-stone-600 text-sm leading-relaxed">{item.desc}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );

  // Halaman 2: Menu & Paket
  const MenuPage = () => (
    <div className="animate-fade-in py-12 md:py-20 bg-stone-50 min-h-screen pb-24 md:pb-12">
      <div className="max-w-6xl mx-auto px-4">
        <div className="text-center mb-12 md:mb-16">
          <h2 className="text-3xl md:text-5xl font-serif font-bold text-emerald-900 mb-4">Pilihan Paket Ekoran</h2>
          <p className="text-base md:text-lg text-stone-600 max-w-2xl mx-auto">
            Praktis, Hemat, dan Berkah. Kami urus semuanya mulai dari penyembelihan hingga pengantaran ke depan pintu Anda.
          </p>
        </div>

        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-20 md:mb-24">
          {aqiqahPackages.map((pkg, idx) => (
            <div key={idx} className={`bg-white rounded-2xl overflow-hidden shadow-lg border flex flex-col transition-all duration-300 hover:shadow-2xl ${pkg.recommended ? 'border-amber-400 ring-2 ring-amber-100' : 'border-stone-200'}`}>
              {pkg.recommended && (
                <div className="bg-amber-400 text-emerald-900 text-center py-2 font-bold text-[10px] md:text-xs uppercase tracking-widest">
                  Paling Laris
                </div>
              )}
              <div className="p-6 md:p-8 flex-grow">
                <h3 className="text-xl md:text-2xl font-bold text-stone-800 mb-2">{pkg.name}</h3>
                <div className="text-2xl md:text-3xl font-bold text-emerald-700 mb-6">{pkg.price}</div>
                <ul className="space-y-3">
                  {pkg.details.map((item, i) => (
                    <li key={i} className="flex items-start gap-3 text-stone-600 text-sm">
                      <CheckCircle className="text-emerald-500 shrink-0 mt-0.5" size={16} />
                      <span>{item}</span>
                    </li>
                  ))}
                </ul>
              </div>
              <div className="p-6 bg-stone-50 border-t border-stone-100">
                <button 
                  onClick={() => setActivePage('contact')}
                  className={`w-full py-3 rounded-xl font-bold text-sm md:text-base transition-all ${pkg.recommended ? 'bg-emerald-800 text-white hover:bg-emerald-900 shadow-md' : 'bg-white border-2 border-emerald-800 text-emerald-800 hover:bg-emerald-50'}`}
                >
                  Pesan Sekarang
                </button>
              </div>
            </div>
          ))}
        </div>

        <div className="text-center mb-10 md:mb-12">
          <h2 className="text-3xl md:text-4xl font-serif font-bold text-emerald-900 mb-4">Paket Kambing Guling</h2>
          <p className="text-base md:text-lg text-stone-600 max-w-3xl mx-auto leading-relaxed">
            Hadirkan kemewahan dalam setiap perayaan dengan sajian eksklusif yang tidak tersedia dalam porsi satuan. 
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
          {eventPackages.map((pkg, idx) => (
            <div key={idx} className="bg-white p-6 md:p-8 rounded-2xl shadow-md border-l-[6px] md:border-l-[8px] border-emerald-800 flex flex-col justify-between transition-all hover:shadow-xl">
              <div>
                <div className="flex justify-between items-start mb-4">
                   <h3 className="text-xl md:text-2xl font-bold text-stone-800">{pkg.name}</h3>
                   <div className="bg-emerald-100 text-emerald-800 px-3 py-1 rounded-full text-[10px] md:text-xs font-bold whitespace-nowrap">
                     {pkg.minOrder}
                   </div>
                </div>
                <p className="text-stone-600 mb-6 text-sm leading-relaxed">{pkg.desc}</p>
              </div>
              <div className="flex flex-col sm:flex-row items-center justify-between gap-4 border-t border-stone-100 pt-6">
                <div className="text-xl md:text-2xl font-bold text-emerald-700">{pkg.price}</div>
                <button 
                  onClick={() => setActivePage('contact')}
                  className="w-full sm:w-auto bg-emerald-800 text-white px-8 py-2.5 rounded-xl text-sm font-bold hover:bg-emerald-900 transition-all shadow-md"
                >
                  Tanya Detail
                </button>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );

  // Halaman 3: Kontak & Lokasi
  const ContactPage = () => {
    const [formData, setFormData] = useState({
      nama: '',
      paket: '',
      wa: '',
      pesan: ''
    });

    const handleInputChange = (e) => {
      const { name, value } = e.target;
      setFormData(prev => ({ ...prev, [name]: value }));
    };

    const handleWhatsAppSubmit = (e) => {
      e.preventDefault();
      const { nama, paket, wa, pesan } = formData;
      const targetPhone = "6285231413408"; 
      
      const text = `Halo CAKSABAR, saya ingin memesan paket sate/gule:\n\n` +
                   `*Nama:* ${nama || '-'}\n` +
                   `*Pilihan Paket:* ${paket || '-'}\n` +
                   `*Nomor WhatsApp:* ${wa || '-'}\n` +
                   `*Pesan Tambahan:* ${pesan || '-'}\n\n` +
                   `Mohon info ketersediaan dan total pembayarannya. Terima kasih.`;
      
      const encodedText = encodeURIComponent(text);
      const whatsappURL = `https://wa.me/${targetPhone}?text=${encodedText}`;
      window.open(whatsappURL, '_blank');
    };

    return (
      <div className="animate-fade-in bg-stone-50 min-h-screen py-8 md:py-16 pb-28 md:pb-16">
        <div className="max-w-5xl mx-auto px-4">
          <div className="bg-white rounded-3xl shadow-2xl overflow-hidden border border-stone-100">
            <div className="grid grid-cols-1 lg:grid-cols-5">
              {/* Sidebar Info */}
              <div className="lg:col-span-2 bg-emerald-900 p-8 md:p-12 text-white flex flex-col justify-between relative overflow-hidden">
                {/* Background Decoration */}
                <div className="absolute -top-10 -left-10 w-40 h-40 bg-white/5 rounded-full blur-2xl"></div>
                <div className="absolute -bottom-10 -right-10 w-40 h-40 bg-amber-400/10 rounded-full blur-2xl"></div>

                <div className="relative z-10">
                  <h2 className="text-3xl md:text-4xl font-serif font-bold mb-6">Hubungi Kami</h2>
                  <p className="mb-10 opacity-80 text-base md:text-lg leading-relaxed">
                    Siap melayani pesanan Anda. Konsultasikan kebutuhan katering Anda bersama tim kami.
                  </p>
                  
                  <div className="space-y-8">
                    <div className="flex items-center gap-5">
                      <div className="w-12 h-12 bg-emerald-800/80 backdrop-blur rounded-2xl flex items-center justify-center border border-emerald-700 shadow-lg">
                        <Phone className="text-amber-400" size={20} />
                      </div>
                      <div>
                        <p className="text-xs opacity-60 uppercase tracking-widest font-bold">Telepon / WhatsApp</p>
                        <p className="text-lg md:text-xl font-bold text-amber-50">0852-3141-3408</p>
                      </div>
                    </div>
                    
                    <div className="flex items-center gap-5">
                      <div className="w-12 h-12 bg-emerald-800/80 backdrop-blur rounded-2xl flex items-center justify-center border border-emerald-700 shadow-lg">
                        <MapPin className="text-amber-400" size={20} />
                      </div>
                      <div>
                        <p className="text-xs opacity-60 uppercase tracking-widest font-bold">Lokasi Warung</p>
                        <p className="text-base md:text-lg font-bold leading-tight">Jl. Brigjen Katamso No.88-81</p>
                        <p className="text-xs opacity-80 mt-1">Tompokersan, Lumajang, Jawa Timur</p>
                      </div>
                    </div>

                    <div className="flex items-start gap-5">
                      <div className="w-12 h-12 bg-emerald-800/80 backdrop-blur rounded-2xl flex items-center justify-center shrink-0 border border-emerald-700 shadow-lg">
                        <Calendar className="text-amber-400" size={20} />
                      </div>
                      <div>
                        <p className="text-xs opacity-60 uppercase tracking-widest font-bold">Jam Operasional</p>
                        <p className="text-base md:text-lg font-bold">11.00 - 21.00 WIB</p>
                        <div className="mt-4 p-4 bg-white/5 border border-white/10 rounded-xl">
                          <p className="text-[11px] leading-relaxed text-amber-200">
                            <span className="font-bold">Info Penting:</span> Warung dapat tutup jika pesanan padat. Simpan nomor kami untuk info operasional terbaru.
                          </p>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>

              {/* Form Section */}
              <div className="lg:col-span-3 p-8 md:p-12">
                <h3 className="text-2xl font-bold text-stone-800 mb-8 border-b border-stone-100 pb-4">Formulir Pesanan</h3>
                <form className="space-y-6" onSubmit={handleWhatsAppSubmit}>
                  <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                      <label className="block text-xs font-bold text-stone-500 uppercase tracking-widest mb-2">Nama Lengkap</label>
                      <input 
                        type="text" 
                        name="nama"
                        value={formData.nama}
                        onChange={handleInputChange}
                        className="w-full bg-stone-50 border-2 border-stone-200 rounded-xl p-3.5 focus:outline-none focus:border-emerald-600 transition-all text-stone-800" 
                        placeholder="Contoh: Bpk. Raharjo" 
                        required
                      />
                    </div>
                    <div>
                      <label className="block text-xs font-bold text-stone-500 uppercase tracking-widest mb-2">Pilihan Paket</label>
                      <select 
                        name="paket"
                        value={formData.paket}
                        onChange={handleInputChange}
                        className="w-full bg-stone-50 border-2 border-stone-200 rounded-xl p-3.5 focus:outline-none focus:border-emerald-600 transition-all text-stone-800"
                        required
                      >
                        <option value="" disabled>-- Pilih Paket --</option>
                        {allPackageNames.map((pkg, idx) => (
                          <option key={idx} value={pkg}>{pkg}</option>
                        ))}
                      </select>
                    </div>
                  </div>
                  <div>
                    <label className="block text-xs font-bold text-stone-500 uppercase tracking-widest mb-2">Nomor WhatsApp Aktif</label>
                    <input 
                      type="tel" 
                      name="wa"
                      value={formData.wa}
                      onChange={handleInputChange}
                      className="w-full bg-stone-50 border-2 border-stone-200 rounded-xl p-3.5 focus:outline-none focus:border-emerald-600 transition-all text-stone-800" 
                      placeholder="08123xxxx" 
                      required
                    />
                  </div>
                  <div>
                    <label className="block text-xs font-bold text-stone-500 uppercase tracking-widest mb-2">Pesan / Detail Acara</label>
                    <textarea 
                      name="pesan"
                      value={formData.pesan}
                      onChange={handleInputChange}
                      className="w-full bg-stone-50 border-2 border-stone-200 rounded-xl p-3.5 h-32 focus:outline-none focus:border-emerald-600 transition-all text-stone-800 resize-none" 
                      placeholder="Tulis tanggal acara, lokasi pengiriman, atau permintaan khusus lainnya..."
                    ></textarea>
                  </div>
                  <button 
                    type="submit"
                    className="w-full bg-emerald-800 hover:bg-emerald-900 text-white font-bold py-4 rounded-2xl text-lg transition-all shadow-xl hover:shadow-emerald-900/20 active:scale-[0.98] flex items-center justify-center gap-3 group"
                  >
                    Kirim Pesanan Sekarang <ArrowRight className="group-hover:translate-x-1 transition-transform" />
                  </button>
                  <p className="text-center text-[10px] text-stone-400 mt-4 uppercase tracking-widest">
                    Pesan akan diteruskan otomatis ke aplikasi WhatsApp
                  </p>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    );
  };

  const Footer = () => (
    <footer className="bg-stone-900 text-stone-400 py-16 hidden md:block">
      <div className="max-w-6xl mx-auto px-4 text-center">
        <h2 className="text-3xl font-serif text-amber-500 font-bold mb-6 tracking-widest">CAKSABAR</h2>
        <p className="mb-10 max-w-2xl mx-auto text-lg opacity-80 leading-relaxed">
          Menghadirkan kenyamanan cita rasa otentik dalam balutan layanan modern yang praktis. Kami mengutamakan kemudahan dan profesionalitas demi menyempurnakan setiap momen berharga Anda.
        </p>
        <div className="border-t border-white/5 pt-10 text-xs uppercase tracking-widest opacity-40">
          &copy; 2024 Warung Sate Caksabar Lumajang. Seluruh Hak Cipta Dilindungi.
        </div>
      </div>
    </footer>
  );

  return (
    <div className="min-h-screen flex flex-col font-sans bg-stone-50 text-stone-800 selection:bg-emerald-100 selection:text-emerald-900">
      <Navbar />
      <main className="flex-grow">
        {activePage === 'home' && <HomePage />}
        {activePage === 'menu' && <MenuPage />}
        {activePage === 'contact' && <ContactPage />}
      </main>
      <Footer />
    </div>
  );
};

export default App;
